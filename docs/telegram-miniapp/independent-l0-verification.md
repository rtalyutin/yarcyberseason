# Независимая проверка L0 — 12.09.2026

Проверяемая область: `src/telegram/config.js`, `src/telegram/contracts.js`, L0 fixtures, контрактные тесты и карта контрактов. Проверяющий: `/root/verify_l0`, только чтение и независимые локальные probes.

## Цикл дефект → исправление → повторная проверка

Первичная проверка вернула L0 на доработку: guard допускал поля исходных данных в `SlotView`, не связывал `slotKey` с выбранным турниром и матчем и проверял DTO не полностью. Реализация была усилена: слоты теперь имеют закрытый набор полей, match slot обязан ссылаться на существующий единый `MatchViewModel`, ключи турнира/матча проверяются, вложенные модели результата, таблиц, этапов, наград и проекта валидируются структурно.

После повторной проверки дополнительно ужесточены массивы `result.maps` и `rewards.additionalAwards`: каждый элемент обязан быть объектом.

## Итоговый verdict

**PASS** — L0-01, L0-02 и L0-03 закрыты.

- `npm run test:telegram`: 9/9 PASS.
- Полная валидная модель принимается без ошибок.
- `maps: [null, 42]` и `additionalAwards: [null, 42]` отклоняются.
- Raw-поля в `SlotView` отклоняются.
- `slotKey` другого турнира или с неверным raw match id отклоняется.
- Контрольные SHA-256 пяти проверяемых файлов совпали до и после probes: проверка не меняла реализацию.

Полный suite и build выполнены отдельно основным исполнителем: 62/62 PASS и build PASS. Browser, Telegram и production не входят в этот gate, поскольку соответствующие реализации ещё не созданы.
