# Независимая проверка Rich Card — шаг 6

20.09.2026, 03:49 UTC. Исполнитель: отдельный агент `/root/verify_rich_card`; исходники реализации не писал и не изменял. Критерии составлены по §13, шагу 6 ТЗ до получения кандидата. Для адаптеров, данных и серверной пробы **локальный gate PASS**. **Полный шаг 6 BLOCKED**: визуальная проверка и работа в настоящих клиентах Telegram не подтверждены.

## Объект и границы

- Проверенный commit: `1971e872ac20116c54a94785289f4248a9496d2d`; tree: `9b39da27c370cdd4b0a2e80401b63f1579ac29db`. Перед проверками и после них рабочая копия чистая; этот отчёт добавлен позднее.
- Исходный кандидат: `c0b15e2d210c754d8984e2b0154647e5d9c58815`. В нём найден RC-QA-1, исправление проверено на указанном итоговом commit.
- Среда: Node `v24.19.0`, локальный Linux. Все HTTP-ответы в испытаниях отправки — собственные mock-объекты. Реальных запросов к Telegram не выполнялось, токен синтетический.
- Прототип SHA-256: `fd34a58b4de7d59fd6cf783eba7543314bbd21a50d2fd75d931d94cf5f83ed9e`.
- Demo fixture SHA-256: `d29a9ec2251d1324a031c10d9271845c8358bba7fd79712c2e54abe5f16e07d2`.
- ТЗ: `/workspace/scratch/4d5e017d884a/rich-card-spec/YarCyberSeason-Telegram-MiniApp-TZ.md`, §13, шаг 6. Локальные карточки, адаптеры и подготовленная проба входят в scope; постоянный бот-сервис, публикация и внешние отправки не входят.

## Исполненные проверки

Независимый набор: **8/8 групп PASS**. Он использует дополнительные данные и наблюдения, а не только авторские тесты.

| Критерий | Независимый сценарий и фактический результат |
| --- | --- |
| Единые факты, ноль и неизвестность | Нормализованная модель с командами `QA <Юг>` / `QA & Север`, BO7, подтверждённым `score=[0,3]`, отличающимися `series=[8,9]` и `sourceScore=[21,6]`. Оба представления показывают 0:3/BO7; чужие поля 8:9/21:6 не просачиваются. При `confirmed=false` либо `known=false` счёт отсутствует; строковое `confirmed='true'` отклоняется. Исходная модель не изменена. |
| Пустой текущий турнир и архив | Текущий Dota: 16 участников, 0 матчей. Архивный ключ в нём отклонён. Опубликованный CS2 остаётся отдельным финалом 2:3; три demo-состояния маркированы отдельно; архиву не приписана текущая карта. |
| Схема API и действия | У всех четырёх карточек только `blocks` в InputRichMessage; строки RichText, heading 1–6, InputMediaPhoto внутри photo/collage, 1–8 кнопок в строке и ровно один тип каждой кнопки. Demo-кнопки отключены; URL «Все матчи» сохраняет правильный турнир. Plain укладывается в 4096 символов и содержит предупреждения о трёх demo-действиях. |
| Текст и URL | `</script><script>alert(0)</script>` остаётся экранированным текстом локального HTML. `javascript:`, `data:`, сторонний origin и URL с credentials отклонены. Rich-представление выключает автоматическое распознавание сущностей. |
| Исход операции и отсутствие повторов | Успех с ID, 429, 500, неполный ответ, чужой chat и timeout: соответственно SUCCEEDED / FAILED / UNKNOWN. Каждый сценарий вызывает mock ровно один раз; timeout не раскрывает токен, автоматического fallback нет. Неразрешённый chat даёт 0 вызовов. |
| Идентичность редактирования | Для edit message_id=17 ответы с 0, -1, 18 либо небезопасным integer дают UNKNOWN. Ответ с 17 даёт SUCCEEDED. Это отдельная перепроверка RC-QA-1. |
| Dry-run CLI | При заданном синтетическом токене и fetch, завершающем процесс при обращении к сети, запуск без `--execute` возвращает PREPARED/network=false, без токена. |
| Журнал попытки CLI | `main()` с mock fetch: до отправки на диске уже IN_PROGRESS; после ответа — SUCCEEDED/messageId=51, requestHash и права 0600. Повтор с тем же receipt отклонён до второго вызова. В receipt и stdout нет токена. |

Дополнительно мной исполнены `node --test tests/*.test.mjs`: **171/171 PASS**, без пропусков; `npm run build`: **PASS**, валидированы 50 команд и 120 матчей, создан Sites build. Diff относительно `74fd55a5c7ae9e4ebfc73502ae1b328c6e2aa94b` ограничен 11 файлами нового прототипа/адаптеров/тестов/документации. Существующие маршруты, рабочие данные, сервер сайта и основные компоненты не изменены.

Проверенные схемы сверены 20.09.2026 непосредственно с [Bot API 10.3](https://core.telegram.org/bots/api), включая [InputRichMessage](https://core.telegram.org/bots/api#inputrichmessage), [RichText](https://core.telegram.org/bots/api#richtext), [InputRichBlockPhoto](https://core.telegram.org/bots/api#inputrichblockphoto), [InputRichBlockButtons](https://core.telegram.org/bots/api#inputrichblockbuttons), [RichMessageButton](https://core.telegram.org/bots/api#richmessagebutton) и [sendRichMessage](https://core.telegram.org/bots/api#sendrichmessage). Это проверка подготовленного payload, не ответ Telegram о его принятии.

Сырые локальные свидетельства: `/tmp/rich-card-qa-independent.json`, `/tmp/rich-card-qa-suite.log`, `/tmp/rich-card-qa-build.log`; независимый runner `/tmp/rich-card-independent.mjs`, SHA-256 `ec14d3acfe7c35f81fdd70c3b4c8fc313197f927776f513d8178365e53c0f19c`. Эти временные файлы не являются поставкой. Для повторения штатной регрессии используются указанные выше команды; входы независимых проверок и наблюдения сохранены в таблице.

Digest сборки: `dist/client/index.html` SHA-256 `1e2bc55c8ad565aef85b6bab978bdbe06339b18cc2b58524767ff94309714897`; `dist/server/index.js` SHA-256 `0e007c170890052d1d8d875f9b148f33899fd28cfb1596aca8c24c4f06291a6c`.

## Найденный и закрытый дефект

**RC-QA-1 — ложный SUCCEEDED при недостоверном message ID.** В `c0b15e2` запрос edit с message_id=17 и ответ `{ok:true,result:{message_id:42,chat:{id:123}}}` возвращал SUCCEEDED; аналогично принимались ID 0/-1. Ожидался UNKNOWN, поскольку редактирование нужного сообщения не доказано. В `1971e87` добавлена проверка положительного безопасного integer и равенства ID для edit. Перепроверка с собственными mock-ответами пройдена. Открытых установленных дефектов в проверенной локальной границе нет.

## Ограничения и передача

- Визуальная геометрия, переключатель сценариев, клики и клавиатура в настоящем браузере — **NOT_VERIFIED**. Координатор сообщил `ERR_BLOCKED_BY_CLIENT` для локального URL; я не обходил это ограничение и не выдаю статическое HTML-чтение за браузерную проверку.
- Telegram Android/iOS/Desktop, старые клиенты, реальные отправка/редактирование, изображения, кнопки и возврат — **NOT_VERIFIED**. Версии клиентов не получены; разрешённого чата и серверного токена нет. Для полного критерия перехода это **BLOCKED**, а не PASS.
- `operation_status=NOT_STARTED` для внешних действий. Успехи mock-проверок не являются доставкой сообщений. Разрешение на выпуск не выдаётся; шаг 7 не открыт.
- Следующее действие: координатор передаёт готовый локальный результат; согласованная проба в Telegram возможна после предоставления тестовой среды и разрешения. Только её наблюдения могут закрыть внешний gate.

`handoff_schema=FEATURE_HANDOFF/1`; `handoff_digest=880ed9d2dc0004f39250f4a5c3739840cc1f9c4b37edf78b652dc787f5aa44e4`; `evidence_status=VERIFIED` для локальной границы; `gate_verdict=PASS` для модели/адаптеров/mock-пробы, `BLOCKED` для полного шага 6; `action_decision=UNDECIDED` для внешней пробы; `hypothesis_assessment=NOT_ASSESSED`; `return_to=/root`.
