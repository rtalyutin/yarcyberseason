# Независимая проверка шага 6: «Итоги»

Дата: 19.09.2026, завершение проверок 16:31 UTC. Исполнитель: отдельный агент `/root/verify_results`; разработчик — `/root`.

**Node/SSR gate: PASS — 8/8 независимых групп и 150/150 проектных тестов. Наблюдаемых дефектов в проверенном объёме нет.** Визуальная и интерактивная браузерная приёмка не выполнена: этот отчёт не закрывает общий UI/Telegram gate и не разрешает публикацию.

## Объект и независимость

- Candidate commit: `7e6c8a8d6eb52f9bd2bdb700102734f0c6682a3e`.
- Candidate tree: `1d685c2eeed06f397a782877d394b518a8f2cbb4`.
- По последующей передаче координатора: сохранённый remote commit `9a8b796ac89722619cbc307cf894f660fd95b804` имеет то же дерево. Независимый прогон относится к указанному локальному candidate; readback remote выполнял координатор.
- База: `d42d0c1dc46193eb181d0604e871137aaabee26a`.
- Перед исполнением в рабочей копии изменён только `docs/telegram-miniapp/STATE.md`. После проверок `git diff --exit-code HEAD -- src tests package.json package-lock.json public scripts worker vite.config.mjs` подтвердил отсутствие незакоммиченных изменений исполняемого объекта.
- ТЗ: `/tmp/ycs-miniapp-tz-current.md`, SHA-256 `6c2b5e949ac242820b3307baca79af421cb4b1d1a27d1df63a1ddd5b66a49039`. Проверены относящиеся к срезу L2, DATA-01/02/04/07/08 и требования AGENTS.md.
- Среда: Linux, Node `v24.19.0`, локальный esbuild + React SSR. Сеть, Telegram и реальные пользователи в сценариях не участвовали.
- Сценарии и собственная трёхкомандная фикстура подготовлены до чтения нового Results UI. Исполнение — через публичные `buildMiniAppModel`, `loadMiniAppModel`, контракты навигации и интегрированный `TournamentScreen`, не через изолированный новый компонент.
- Фикстура явно синтетическая: победа второй стороны `0:2`, исходный массив мест `3/1/2`, отдельные canonical names и названия участия. Исходные JSON и source-файлы проверяющий не менял.

## Исполненные проверки

| Сценарий | Наблюдаемое поведение | Результат |
|---|---|---|
| Фактический турнир без итогов | 16 участников; `results=null`, вкладки нет; `?section=results`, start target и преобразование `#results` возвращают overview | PASS |
| Валидные места и подтверждённый финал | Места отображены `1/2/3`, команды взяты из опубликованных названий; canonical names не подставляются; серия `0:2`; исходные tournament/registry не изменены | PASS |
| Только `finalMatchId`, без placements | Финал виден; места и третий участник не достраиваются; свойство placements остаётся отсутствующим | PASS |
| Непригодные итоги | Отклонены `{}`, массив results, отсутствующий/скрытый финал, false/отсутствующий resultConfirmed, scheduled, unknown и конфликт чемпиона | PASS |
| Нарушенные места | Отклонены посторонняя команда, повтор команды, ошибочный финалист на втором месте, повтор позиции и позиция 0 | PASS |
| Технический финал | Видна подпись технической победы; сыгранные карты и отсутствующие сведения не создаются | PASS |
| Один normalized result на трёх поверхностях | Изменение score/series на различающий sentinel `[31,17]` проявляется в Матчах, плей-офф и Итогах | PASS |
| Текст и границы маршрута | HTML-символы опубликованного названия команды экранируются; другой tournament id отклонён; `/tg/results` не становится третьим экраном; чужой query tournament удаляется при нормализации | PASS |

Независимый исполняемый набор: `node --test /tmp/ycs-results-independent.mjs` — 8 passed, 0 failed. SHA-256 набора: `73f8cbacd0fb3e493f149de4ef1044f02431a2f80f1146a6a81249107e7706f5`.

Общая регрессия самостоятельно исполнена проверяющим: `node --test tests/*.test.mjs` — 150 passed, 0 failed. `git diff --check` — PASS.

Сравнение с базой подтвердило отсутствие изменений в `src/data`, `src/lib`, L0 contracts/config/router, public, зависимостях, scripts, worker и Vite config. Сохраняется существующая конфигурация двух маршрутов и rift/ru; это статическая проверка границ, не визуальная приёмка.

## Ограничения и корректировка проверки

Первый независимый запуск дал 7/8: проверяющий ошибочно ожидал `tournament.title` в шапке, где согласованный интерфейс использует `season`. Проверка исправлена на экранирование названия команды, реально отображаемого в Итогах; соответствующие participant, binding, final и placement согласованы в фикстуре. Исходник приложения не менялся. Исходный неудачный лог сохранён отдельно от финального.

По наблюдению координатора в текущем запуске: после восстановления browser bootstrap новая tab8 при открытии `http://127.0.0.1:4175/tg/tournament?section=results` вернула `ERR_BLOCKED_BY_CLIENT`; повторов и обхода не было. Этот проверяющий браузерную границу не обходил. Размеры 360/390/430/768 и широкие экраны, переполнение длинных названий, фокус, реальные клики/раскрытие, hydration, safe areas, reduced motion и Telegram-клиенты — **NOT_VERIFIED**. SSR доказывает вывод и данные, но не геометрию и интерактивность.

Локальные воспроизводимые материалы текущей сессии, без обещания долговременного хранения: `/tmp/ycs-results-independent.mjs`, `/tmp/ycs-results-independent.log` (первый запуск), `/tmp/ycs-results-independent-final.log`, `/tmp/ycs-results-independent-regression.log`, `/tmp/ycs-results-independent-valid.html`, `/tmp/ycs-results-independent-technical.html`.

## Передача

- `handoff_schema`: `FEATURE_HANDOFF/1`; `handoff_digest`: `880ed9d2dc0004f39250f4a5c3739840cc1f9c4b37edf78b652dc787f5aa44e4`.
- `feature_id`: `telegram-step6-results`; `contract_revision`: запрос «реализуй» после предложения следующего среза «Итоги».
- `evidence_status`: `VERIFIED` для указанного Node/SSR gate; `gate_verdict`: `PASS`. Общий визуальный/Telegram gate: `BLOCKED` из-за отсутствующего evidence, без установленного UI-дефекта.
- `action_decision`: `CONTINUE` к фиксации подготовленного среза и отдельной оставшейся UI-проверке; `hypothesis_assessment`: `NOT_ASSESSED` — пользовательский эффект не измерялся.
- `exceptions`: не приняты; `operation_status` внешней публикации: `NOT_STARTED`; разрешение на выпуск этот отчёт не создаёт.
- `next_action` / `return_to`: координатор `/root` сохраняет отчёт с версией; визуальная/Telegram приёмка остаётся открытой. Реальных итогов турнира по-прежнему нет, поэтому пользовательскую вкладку нельзя демонстрировать как опубликованный результат.
