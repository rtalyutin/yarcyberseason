# Шаг 5/8 — совместная проверка Mini App

Статус на 16.09.2026: **ACCEPTED_USER — шаг 5 закрыт**. Роман: «Я проверил, бот отдает сайт в тг. Все корректно». Подтверждён корректный запуск опубликованного сайта через бота в Telegram. Отдельные сценарии ниже и версии устройств не аттестованы этим общим сообщением; подробная проверка остаётся на шаге 7. Ассистент не заявляет собственную проверку всех Telegram-клиентов. Следующий шаг — 6.

## Какая версия проверяется

- Ветка: `codex/telegram-miniapp`.
- Код/данные: интеграция шага 4 с актуальным main `58c9754f7035d04a24ad8dcc511988705d015537`. Точный remote commit фиксируется при размещении в STATE.md; хеши проверенной сборки уже указаны там. Исторический a60052e не публикуется поверх обновлённого сайта.
- Два экрана: `/tg` и `/tg/tournament`, только Dota `dota2-autumn-2026`, 10–25 октября 2026.
- «Разлом» и русский; переключатели скрыты до второго доступного варианта. Реестры и сохранение предпочтений остаются.
- Выбран `botUsername: "YarCyberSeason_bot"`; оформление, два экрана и границы шага 4 сохранены.
- Новые меню, страницы, регламент и архивные ссылки main объединены и проверены на сохранность. Шаг 6 не выполняется до совместной приёмки.

## Что можно открыть

Пользователь выбрал существующий домен: `https://xn--90aiaibl0ahlel5n.xn--p1ai`, путь `/tg`, бот `@YarCyberSeason_bot`. Отдельный Sites не используется из-за недоступности. Размещение текущих двух экранов разрешено 15.09.2026.

Рабочая ссылка проверена: https://xn--90aiaibl0ahlel5n.xn--p1ai/tg. PR #3 слит в main, merge commit `2800ce2e4aa48642ffe0f86343b2ffb0b160bc40`. HTTP 200, опубликованные файлы совпали по SHA-256 с build; браузерные переходы и прямой вход проверены. Роман подтвердил, что бот открывает сайт в Telegram корректно. Ассистент настройки BotFather не менял.

Сохранённый снимок интерфейса шага 4 — ориентир, а не интерактивная проверка:

![Закреплённая версия: главная и участники](./step4-preview.jpg)

Исполнитель размещает интегрированную проверенную сборку на согласованном действующем домене, проверяет HTTPS и байты JS/CSS и только затем передаёт Роману фактические ссылки:

| Назначение | Путь относительно согласованного HTTPS origin |
| --- | --- |
| Главная | `/tg` |
| Прямой вход в участников | `/tg/tournament?section=participants` |
| Браузерная проверка startapp | `/tg?startapp=participants` |

Рабочая ссылка Mini App: https://t.me/YarCyberSeason_bot?startapp. Браузерный параметр сам по себе не доказывает работу Telegram start_param.

## Короткий проход вместе с Романом

До начала записать код сборки, фактический URL, ОС/устройство, версию Telegram или браузера, viewport/ориентацию, сеть и дату. Не записывать bot token, initData и персональные идентификаторы Telegram.

| № | Действие Романа | Что вместе наблюдаем | Факт |
| --- | --- | --- | --- |
| 1 | Открыть главную | Dota 2, 10–25 октября, 16/16, регистрация закрыта, «Разлом», русский | НЕ ПРОВЕРЕНО |
| 2 | «Открыть турнир» → «Участники» | Все 16 команд; список ниже совпадает с интерфейсом | НЕ ПРОВЕРЕНО |
| 3 | Нажать «Назад» | Главная Mini App, без перехода в архив/страницу команды | НЕ ПРОВЕРЕНО |
| 4 | Открыть прямую ссылку на участников → «Назад» | Правильный турнир и возврат на `/tg`, даже без истории главной | НЕ ПРОВЕРЕНО |
| 5 | Открыть launch участников → назад → перезагрузить | Участники открылись один раз; после возврата reload не уводит туда снова | НЕ ПРОВЕРЕНО |
| 6 | Закрыть и повторно открыть приложение обычным запуском | Нет зависания/двойного перехода; русский и «Разлом» сохранены | НЕ ПРОВЕРЕНО |
| 7 | Просмотреть на телефоне, прокрутить до конца, повернуть экран | Текст/кнопки доступны; нет горизонтального скролла, наложений и обрезанных участников | НЕ ПРОВЕРЕНО |
| 8 | Запустить из настоящего Telegram | Контент загружается, Telegram BackButton возвращает домой и скрывается там | НЕ ПРОВЕРЕНО |
| 9 | Свернуть/развернуть Telegram, изменить высоту и ориентацию | Нет перекрытия кнопками Telegram/вырезом/нижней системной областью; последний участник доступен | НЕ ПРОВЕРЕНО |

На Android, iOS, Desktop результаты вести раздельно; недоступный клиент не получает PASS от проверки другого. Полная матрица и 24 сценария относятся к шагу 7. Для шага 5 обязательно реальное совместное наблюдение в доступном клиенте Telegram, фиксация оставшихся сред и явное подтверждение Романа.

Фактический порядок из JSON закреплённой сборки: Vnext; Leto Jr; Mi Ne Pushim!; strela team; WAYPROD.; Fummo; TEAM SPERMINT; ARB Esports; PIVNAYA KEGA; Tech Titans; Easy Gaming; Team Borisogleb; parallax team; liqa sto; psb_bank; РГАТУ. Регистр `psb_bank` сохранён из источника, не исправлен догадкой. Матчей 0; пустые слоты не считаются результатами.

Другие разделы ещё готовятся — это известная граница шага 4, не причина начинать шаг 6 до приёмки. Не обещать полный турнир/обновление версии.

## Как фиксируем замечание и закрываем шаг

Для каждого отклонения: номер сценария → шаги → ожидаемое → фактическое → скриншот без личных данных → версия/устройство. Исполнитель воспроизводит дефект, исправляет только затронутый сценарий, сохраняет новую версию и повторяет проверки. При смене кода старая идентичность сборки и её evidence не переносятся автоматически.

Явное подтверждение Романа получено 16.09.2026 и закрывает шаг 5. Оно подтверждает корректный запуск в Telegram; подробные действия и платформы из таблицы не получают автоматически PASS. Успех unit/SSR/build, снимок и согласие verifier остаются отдельными доказательствами. Переход к шагу 6 разрешён ранее согласованным планом.

## Подготовительная проверка интегрированной версии

1. Сверить source, рабочее дерево и GitHub main; сохранить новые изменения сайта.
2. Выполнить `npm run build` и `node --test tests/*.test.mjs` (после конфигурационной поправки достаточно затронутого Telegram-набора, если полный набор уже выполнен на том же коде).
3. Сверить опубликованные `index.html`, entry JS и CSS с build hashes в STATE.md. HTTP 200 от прежней версии не подтверждает обновление.
4. Проверить браузером `/tg` и прямой `/tg/tournament?section=participants`, затем совместно в Telegram.

`node scripts/telegram-review-check.mjs` — историческая проверка a60052e; на новой интеграции ожидаемо отвергает отличающийся source. Не обновлять его эталон ради PASS и не использовать вместо новой приёмки.

Выбор домена и бота сохранён, пользовательская приёмка получена. Ежедневная задача возобновлена с шага 6; будущая публикация новых изменений требует отдельного разрешения.
